/**
 * @fileoverview Firestore Security Rules for Secure Talk application.
 *
 * Core Philosophy:
 * This ruleset enforces a strict user-ownership model for user profiles and
 * contacts. Chats and Groups have shared access via participant lists.
 * Team member profiles are publicly accessible.
 *
 * Data Structure:
 * - /users/{userId}: Stores user profile data, accessible only to the user.
 * - /usernames/{username}: Maps usernames to user IDs to prevent duplicates.
 *   Writeable by anyone to "claim" a username.
 * - /team/{userId}: Stores public team member profiles.
 * - /chats/{chatId}: Stores 1-on-1 chat metadata, accessible to participants.
 * - /chats/{chatId}/messages/{messageId}: Stores messages within a 1-on-1 chat, accessible to participants.
 * - /users/{userId}/contacts/{contactId}: Stores a user's contacts, accessible only to the user.
 * - /groups/{groupId}: Stores group chat metadata, accessible to participants.
 * - /groups/{groupId}/messages/{messageId}: Stores messages within a group chat, accessible to participants.
 *
 * Key Security Decisions:
 * - User listing is disallowed to prevent enumeration.
 * - Team member profiles are publicly readable.
 * - Username "claiming" (writing to /usernames/{username}) is allowed by any authenticated user.
 * - The default security posture for ambiguous relationships is strict owner-only access.
 *
 * Denormalization for Authorization:
 * - Chats and Groups store participant lists directly on the document to avoid costly `get()` calls during rule evaluation.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Secure user profiles.
     * @path /users/{userId}
     * @allow (read) Authenticated user can read their own profile.
     * @allow (create) Authenticated user can create their own profile if the userId matches their auth UID.
     * @allow (update) Authenticated user can update their own profile if the userId matches their auth UID.
     * @deny (delete) No user can delete their profile directly.
     * @deny (list) No user can list all users.
     * @principle Enforces document ownership for writes.
     */
    match /users/{userId} {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }
      allow get: if isOwner(userId);
      allow list: if false;
      allow create: if isOwner(userId) && request.resource.data.id == userId;
      allow update: if isOwner(userId) && resource.data.id == userId;
      allow delete: if false;
    }

    /**
     * @description Secure unique usernames.
     * @path /usernames/{username}
     * @allow (create) Authenticated user can create a username entry.
     * @allow (update) Authenticated user can update a username entry.
     * @allow (get)  Allow anyone to check a username's availability.
     * @allow (list)  Allow anyone to list usernames.
     * @deny (delete) No user can delete a username entry.
     * @principle Allows anyone to claim a username by writing their UID.
     */
    match /usernames/{username} {
      function isSignedIn() {
        return request.auth != null;
      }
      allow get, list: if true;
      allow create, update: if isSignedIn();
      allow delete: if false;
    }

    /**
     * @description Secure team member profiles.
     * @path /team/{userId}
     * @allow (read) Allow anyone to read team member profiles.
     * @deny (create, update, delete) Prevent non-admins from modifying team data. // TODO: Add role-based access control for team management.
     * @deny (list) Prevent listing all team members for security reasons
     * @principle Public read access, owner-only writes (not yet implemented).
     */
    match /team/{userId} {
      allow get: if true;
      allow list: if false;
      allow create, update, delete: if false; // TODO: Add role-based access control for team management.
    }

    /**
     * @description Secure 1-on-1 chat metadata.
     * @path /chats/{chatId}
     * @allow (read) Allow participants to read chat metadata.
     * @allow (create) Allow participants to create a chat.
     * @allow (update) Allow participants to update chat metadata.
     * @deny (delete) No user can delete a chat directly.
     * @deny (list) Prevent listing all chats.
     * @principle Shared access via participant list.
     */
    match /chats/{chatId} {
      function isParticipant() {
        return request.auth != null && request.auth.uid in resource.data.participants;
      }
      allow get: if isParticipant();
      allow list: if false;
      allow create: if request.auth != null && request.resource.data.participants[request.auth.uid] == true;
      allow update: if isParticipant();
      allow delete: if false;
    }

    /**
     * @description Secure messages within a 1-on-1 chat.
     * @path /chats/{chatId}/messages/{messageId}
     * @allow (read) Allow participants to read messages.
     * @allow (create) Allow participants to create messages.
     * @deny (update, delete) No user can update or delete messages directly.
     * @deny (list) Prevent listing all messages.
     * @principle Shared access via parent chat's participant list.
     */
    match /chats/{chatId}/messages/{messageId} {
      function isChatParticipant(chatId) {
          return get(/databases/$(database)/documents/chats/$(chatId)).data.participants[request.auth.uid] == true;
      }
      allow get: if request.auth != null && isChatParticipant(chatId);
      allow list: if false;
      allow create: if request.auth != null && isChatParticipant(chatId);
      allow update, delete: if false;
    }

    /**
     * @description Secure contact information for a user.
     * @path /users/{userId}/contacts/{contactId}
     * @allow (read) Authenticated user can read their own contacts.
     * @allow (create) Authenticated user can create contacts if the userId matches their auth UID.
     * @allow (update) Authenticated user can update their own contacts if the userId matches their auth UID.
     * @deny (delete) No user can delete contacts unless they own them.
     * @allow (list) Authenticated user can list their own contacts.
     * @principle Enforces document ownership.
     */
    match /users/{userId}/contacts/{contactId} {
      function isOwner(userId) {
        return request.auth != null && request.auth.uid == userId;
      }
      allow get: if isOwner(userId);
      allow list: if isOwner(userId);
      allow create: if isOwner(userId) && request.resource.data.userId == userId;
      allow update: if isOwner(userId) && resource.data.userId == userId;
      allow delete: if isOwner(userId);
    }

    /**
     * @description Secure group chat metadata.
     * @path /groups/{groupId}
     * @allow (read) Allow participants to read group metadata.
     * @allow (create) Allow participants to create group.
     * @allow (update) Allow participants to update group metadata.
     * @deny (delete) No user can delete a group directly.
     * @deny (list) Prevent listing all groups.
     * @principle Shared access via participant list.
     */
    match /groups/{groupId} {
      function isParticipant() {
        return request.auth != null && request.auth.uid in resource.data.participants;
      }
      allow get: if isParticipant();
      allow list: if false;
      allow create: if request.auth != null && request.resource.data.participants[request.auth.uid] == true;
      allow update: if isParticipant();
      allow delete: if false;
    }

    /**
     * @description Secure messages within a group chat.
     * @path /groups/{groupId}/messages/{messageId}
     * @allow (read) Allow participants to read messages.
     * @allow (create) Allow participants to create messages.
     * @deny (update, delete) No user can update or delete messages directly.
     * @deny (list) Prevent listing all messages.
     * @principle Shared access via parent group's participant list.
     */
    match /groups/{groupId}/messages/{messageId} {
      function isGroupParticipant(groupId) {
          return get(/databases/$(database)/documents/groups/$(groupId)).data.participants[request.auth.uid] == true;
      }
      allow get: if request.auth != null && isGroupParticipant(groupId);
      allow list: if false;
      allow create: if request.auth != null && isGroupParticipant(groupId);
      allow update, delete: if false;
    }
      /**
     * @description Administrative page to manage team members.
     * @path /admin/team
     * @allow (read) No user can read team members profiles.
     * @deny (create, update, delete) No user can modify team data. // TODO: Add role-based access control for team management.
     * @deny (list) No user can list all team members profiles for security reasons
     * @principle Access only by admin roles (not implemented yet).
     */
       match /admin/team {
        allow get, list: if false;
        allow create, update, delete: if false; // TODO: Add role-based access control for team management.
      }
  }
}